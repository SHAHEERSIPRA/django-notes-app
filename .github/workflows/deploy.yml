name: Deploy Django Notes App to EC2 via ECR

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: django-notes-app-prod
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout source
        uses: actions/checkout@v4

      # 2️⃣ Configure AWS credentials
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      # 3️⃣ Set Docker image tag
      - name: Set IMAGE_TAG
        id: vars
        run: echo "IMAGE_TAG=${GITHUB_SHA}" >> $GITHUB_OUTPUT

      # 4️⃣ Build and push Docker image to ECR
      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ secrets.ECR_REGISTRY }}
          ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
          IMAGE_TAG: ${{ steps.vars.outputs.IMAGE_TAG }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
        run: |
          # Convert repository name to lowercase
          REPO=$(echo $ECR_REPOSITORY | tr '[:upper:]' '[:lower:]')

          # Login to ECR in this step
          aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $ECR_REGISTRY

          # Build Docker image
          docker build -t $ECR_REGISTRY/$REPO:$IMAGE_TAG .

          # Tag latest
          docker tag $ECR_REGISTRY/$REPO:$IMAGE_TAG $ECR_REGISTRY/$REPO:latest

          # Push both tags
          docker push $ECR_REGISTRY/$REPO:$IMAGE_TAG
          docker push $ECR_REGISTRY/$REPO:latest

      # 5️⃣ Deploy to EC2 via SSH
      - name: Deploy on EC2
        uses: appleboy/ssh-action@v1.2.2
        env:
          ECR_REGISTRY: ${{ secrets.ECR_REGISTRY }}
          ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
          IMAGE_TAG: ${{ steps.vars.outputs.IMAGE_TAG }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          APP_DIR: ${{ secrets.APP_DIR }}
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          envs: ECR_REGISTRY,ECR_REPOSITORY,IMAGE_TAG,AWS_REGION,APP_DIR
          script: |
            set -e

            mkdir -p $APP_DIR
            cd $APP_DIR

            # Clone or update repo
            if [ ! -d .git ]; then
              git clone https://github.com/${{ github.repository }} .
            else
              git fetch --all
              git reset --hard origin/main
            fi

            # Login to ECR
            aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $ECR_REGISTRY

            # Create docker-compose.prod.yml
            cat > docker-compose.prod.yml <<EOF
            version: '3.9'

            services:
              nginx:
                build: ./nginx
                image: nginx:local
                container_name: nginx_cont
                ports:
                  - "80:80"
                restart: always
                depends_on:
                  django_app:
                    condition: service_healthy
                networks:
                  - notes-app-nw

              django_app:
                image: $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
                container_name: django_cont
                ports:
                  - "8000:8000"
                command: >
                  sh -c "
                  sleep 20;
                  python manage.py migrate --noinput &&
                  gunicorn notesapp.wsgi:application --bind 0.0.0.0:8000
                  "
                env_file:
                  - .env
                depends_on:
                  db:
                    condition: service_healthy
                restart: always
                healthcheck:
                  test: ["CMD-SHELL", "curl -f http://localhost:8000/admin || exit 1"]
                  interval: 10s
                  timeout: 5s
                  retries: 5
                  start_period: 40s
                networks:
                  - notes-app-nw

              db:
                image: mysql:8.0
                container_name: db_cont
                environment:
                  MYSQL_ROOT_PASSWORD: root
                  MYSQL_DATABASE: test_db
                volumes:
                  - ./data/mysql/db:/var/lib/mysql
                healthcheck:
                  test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -proot --silent"]
                  interval: 10s
                  timeout: 5s
                  retries: 10
                  start_period: 40s
                restart: always
                networks:
                  - notes-app-nw

            networks:
              notes-app-nw:
            EOF

            # Pull, stop old containers, rebuild and start
            docker compose -f docker-compose.prod.yml down || true
            docker compose -f docker-compose.prod.yml pull django_app || true
            docker compose -f docker-compose.prod.yml up -d --build

            # Clean unused images
            docker image prune -af
